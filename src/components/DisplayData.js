import React from 'react'

function DisplayData({ firstName, lastName }) {
    return (
        <>
            <h1>{firstName}</h1>
            <h1>{lastName}</h1>

        </>
    )
}

export default DisplayData